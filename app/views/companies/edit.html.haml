%h2 Edit Company
= form_for @company, url: {action: "update"} do |f|
  = f.text_field :name
  = f.file_field :image
  = f.submit

= link_to '<< Back to index page', :controller => 'home', :action => 'index'